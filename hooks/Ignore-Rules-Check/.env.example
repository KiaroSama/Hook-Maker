# Ignore-Rules-Check configuration. Copy to ".env" and edit if needed.
EVENTS=SessionStart,Stop
TARGET_PROJECTS=
CLIENTS=Both

# Optional extra gitignore patterns, separated by semicolons.
EXTRA_PATTERNS=
