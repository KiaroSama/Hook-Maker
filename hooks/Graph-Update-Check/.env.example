# Graph-Update-Check configuration.
# Copy this file to ".env" (same folder) and edit. ".env" is git-ignored.

# Events to register on (comma separated). Runs after each task.
EVENTS=Stop

# Project roots for config-based install from the Hook Maker menu (semicolon separated).
TARGET_PROJECTS=

# Which client(s) to install for from the Hook Maker menu: Claude, Codex, Kiro,
# or All. Legacy: Both = Claude + Codex only.
CLIENTS=Both

# Minimum minutes between reminders per project.
COOLDOWN_MINUTES=60

# Where Codebase Memory MCP keeps its per-project databases. Read because this
# hook stays SILENT when the project already has a CBM index - Graph-Read-Check
# makes the same call, and the pair must agree about which graph is primary.
# Blank means the CBM_CACHE_DIR environment variable, then the binary default.
CBM_CACHE_DIR=
