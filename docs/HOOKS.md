# Shipped hooks — full per-hook reference

Moved verbatim out of `README.md` (which keeps the overview and the fixed menu list): these
per-hook descriptions are the complete behaviour contract for each shipped hook and grew far
past what a front page can carry.

**One client fact decides the shape of every Stop-time advisory in this set.** On Claude Code a Stop
hook's `hookSpecificOutput.additionalContext` is not a passive note: the client re-invokes the model
with it (observed in Claude Code 2.1.260, 2026-09-07 — a `hook_additional_context` transcript entry
followed by a fresh assistant turn seconds later, with no user input between them). A Stop advisory
is therefore a soft block and is bounded exactly like one — once per session per unchanged state —
because an advisory repeated on every Stop re-invokes the agent on every Stop: it answers, stops, is
re-invoked, answers again, until the user interrupts. Requirements that concern the *closing reply*
(`MCP used:`, `Skills used:`, the session summary) are delivered **before** the task instead, at
SessionStart and on the prompt, so the reply that ends the task carries them with no extra turn.


| Hook | Runs | What it does |
| --- | --- | --- |
| `Cross-Project-.ai-Knowledge-Sync` | pre-task (SessionStart, UserPromptSubmit) | The sync engine: stages changed `.ai` knowledge from related projects for review. |
| `Git-Sync-Check` | pre-task + post-task (Stop, SubagentStop) | Reports uncommitted/unpushed/unpulled work at SessionStart/UserPromptSubmit as non-blocking context (never modifies the repository); SessionStart also silently captures a sanitized baseline of the repo's worktrees/local branches. On Stop/SubagentStop it is a detection-and-instruction boundary, not an executor — the hook itself never runs `git add`/`commit`/`pull`/`push`/`merge`/`rebase`/`branch -d`/`worktree remove`; instead it gives the agent a **mandatory** operational instruction to reconcile the repository now (stage this task's verified changes, commit with a neutral message, push), and to reconcile any branch/worktree it created or changed during the task (merge/push into the intended destination; remove a worktree only once its purpose is complete), instead of waiting for a separate user request — unless doing so is unsafe (failing tests, incomplete work, unrelated pre-existing changes, secrets/protected files, a merge/rebase/conflict state, or an authorization/permission block), in which case the agent preserves the work and reports why. A branch/worktree that existed unchanged before the task is advisory context only, never a blocker, and a branch that is pushed to its own upstream but not yet merged is not itself a blocker either (this hook never infers that every remote branch must be merged). A real git inspection runs on every Stop/SubagentStop (never a blind time-only skip; the worktree/branch scan is bounded and degrades honestly); a genuine block is `decision:block` for both clients, while a non-blocking advisory (e.g. a locked/prunable worktree) is client-aware (`additionalContext` for Claude, `systemMessage` for Codex). Repeat output is gated by a fingerprint of the actionable state (branch/HEAD/upstream/ahead-behind/status plus any task-scoped worktree/branch findings) combined with the session id, so a given state is reported once per session, while a changed state or a new session is evaluated and reported again immediately. |
| `Feature-Request-Check` | pre-task (UserPromptSubmit) + post-task (Stop, SubagentStop) | The only shipped hook that gates on a **skill chain**. `UserPromptSubmit` is advisory: when a prompt reads as a feature request it says to run the mattpocock chain — `grilling` + `domain-modeling`, then the spec, then tickets, then implement ticket by ticket (tdd → code-review → commit) — instead of coding straight from the prompt. Detection is deliberately conservative: an explicit phrase (`feature`, `feature request`, `as a user`, `should be able to`, `make it possible`, and the Persian equivalents) is decisive on its own, while a build verb (`add`/`implement`/`build`/`create`/`introduce`/`support`) counts only when it has an object within 60 characters **and** the prompt carries no bug vocabulary — so “fix the crash by adding a guard” stays a bug fix. On `Stop`/`SubagentStop` it reads the session transcript and **blocks once** when a feature prompt was seen and no `Skill` call to `grilling` appears in it. It never blocks without evidence: no `transcript_path` (Codex, Kiro, an unknown client) and a *partial* transcript read are both silent — no claim in either direction, and never a false “all clear”. It also never counts its own advisory banner as the feature prompt. The block is fingerprinted over the **set** of feature prompts, so the same unfinished business passes silently on the next Stop while a genuinely new feature prompt re-arms it — and “this was not a feature” plus one line of reasoning is a complete answer that clears it. On Kiro, which cannot block at Stop, the same text goes out as advice, labelled as the degraded stop gate rather than pretended away. `.env`: `MAX_TRANSCRIPT_BYTES` (default `20000000`; the transcript is read from the **start**, where the feature prompt and the grilling call both are, and values below `100000` are ignored). |
| `Ai-Memory-Load` (menu: `Ai-Context-Load`) | pre-task (SessionStart, UserPromptSubmit) | Loads `.ai/memory.md` (the startup router) directly into context and lists every other top-level `.ai/*.md` file by name, so the agent can route to the relevant project context without loading unrelated files. Fingerprinted on the router content plus the file set; capped with a truncation note; silent when no `.ai/memory.md` exists. |
| `Ai-Memory-Check` (menu: `Ai-Context-Check`) | post-task (Stop) | If the project's `.ai` context is stale, asks the AI to update whichever existing `.ai/*.md` files are relevant — or finish if nothing durable was learned. |
| `Graph-Read-Check` | pre-task (SessionStart, UserPromptSubmit) | If `graphify-out/graph.json` exists, the SessionStart note gives the compact graph-availability policy once; `UserPromptSubmit` reminds again **only when the prompt itself suggests codebase structure/dependency/call-path/architecture/broad-impact understanding is needed** — in **English or Persian** (معماری/ساختار/وابستگی/مسیر فراخوانی/ماژول/نقطه ورود/بازنویسی/کل کدبیس …) — fingerprinted per session + graph version so an unchanged reminder isn't repeated. When **no** graph exists but the task materially needs codebase-wide understanding, it advises creating one first (a bounded, non-executing `graphify` availability check, then create-then-query) or, if Graphify is unavailable, one bounded fall-back to targeted source inspection; it stays silent for docs/config/local edits. Detector/advisory only — it never runs Graphify or project code.  **Yields to Codebase Memory:** when `<cache>\_config.db` exists and this project has a CBM index, this hook stays silent — that index is then the primary code graph and `Cbm-Read-Check`/`Cbm-Update-Check` own the message. `.env`: `CBM_CACHE_DIR` (blank = the `CBM_CACHE_DIR` environment variable, then the binary's own default). |
| `Graph-Update-Check` | post-task (Stop) | If `graphify-out/graph.json` is stale, asks the AI to decide whether `graphify update .` is warranted based on **structural impact** (added/removed/renamed symbols, changed exports/imports/call/inheritance relationships, new entry points, changed cross-file dependencies) — never on how many files changed: a one-file change can still be graph-relevant while a multi-file change (docs/comments/formatting-only, literal/config values, generated output) can be graph-irrelevant.  **Yields to Codebase Memory:** when `<cache>\_config.db` exists and this project has a CBM index, this hook stays silent — that index is then the primary code graph and `Cbm-Read-Check`/`Cbm-Update-Check` own the message. `.env`: `CBM_CACHE_DIR` (blank = the `CBM_CACHE_DIR` environment variable, then the binary's own default). |
| `Cbm-Read-Check` | pre-task (SessionStart, UserPromptSubmit) | **Advisory only** — never blocks, never runs the Codebase Memory binary (measured at ~1.9 s per call, which no hook budget affords), and never writes outside its own state file. It looks at the filesystem alone: `<cache>\_config.db` to learn whether Codebase Memory MCP exists on this machine at all, and `<cache>\<project>.db` to learn whether *this* project is indexed — the same reason the Graphify hooks only test for `graphify-out/graph.json`. Completely silent when the server was never set up. For an indexed project it says to query the graph **before** browsing files (`get_architecture` for orientation, `search_graph` for symbols, `trace_path` for callers/callees, `get_code_snippet` for exact source, `detect_changes` for blast radius) and that coverage is best-effort, never proof — `check_index_coverage` every cited path and confirm findings in the source. For a project with no index it hands over the exact one-time `index_repository(repo_path="…", mode="moderate")` call: local, seconds, and a background watcher keeps it fresh afterwards. `UserPromptSubmit` speaks only for a prompt that needs codebase-wide understanding, using the *same* relevance test as `Graph-Read-Check` so the two can never disagree about the same prompt; one message per session per index state, so indexing during the session still gets the follow-up “now query it”. **Coordination:** when Codebase Memory is installed *and* this project is indexed, `Graph-Read-Check` and `Graph-Update-Check` stay silent — the CBM index is then the primary code graph, and two hooks pointing at two different graphs is noise rather than redundancy. Known limitation, deliberately not worked around: a caller may override the project name with `index_repository(name=…)`, which a hook cannot see, so such a project reads as un-indexed here — it fails toward suggesting an index that already exists, never toward a wrong claim about the code. `.env`: `CBM_CACHE_DIR` (blank = the `CBM_CACHE_DIR` environment variable, then the binary's own default `%USERPROFILE%\.cache\codebase-memory-mcp`). |
| `Cbm-Update-Check` | post-task (Stop, SubagentStop) | **Advisory only** — never blocks, never runs the CBM binary, never writes outside its own state file. It mirrors `Graph-Update-Check` step for step with the artifact swapped: `stop_hook_active` guard → the index exists → per-project cooldown → newest real work versus the index's last write. “Newest work” comes from **git**, not file mtimes, so a checkout, a formatter or an editor save does not count as a change. **Expect this hook to be silent almost always, and read that as correct behaviour:** Codebase Memory runs a background watcher that rewrites the project database incrementally, so a gap only opens when the watcher is stopped, wedged, or was never given this project. When it does report, it names both timestamps, says to call `index_status` and re-run `index_repository` if the change is not reflected, and leaves the judgement where it belongs — **structure** (a changed symbol, export, import, call/inheritance edge or entry point) matters; docs, formatting and generated output do not, and skipping it is then the right answer. A project with **no** index at all is `Cbm-Read-Check`'s message at the start of a session where it is actionable, not this hook's at the end. `.env`: `CBM_CACHE_DIR`; `STALE_MINUTES` (default `10` — the watcher's expected catch-up time, not a patience setting: below it the watcher simply has not caught up yet); `COOLDOWN_MINUTES` (default `60`). |
| `Synapse-Rules-Check` | pre-task (SessionStart) + post-task (Stop/SubagentStop) | Keeps the agent's own operating rules current from the **Synapse** memory store. It is ADVISORY and holds no MCP client of its own — a PowerShell hook cannot call an MCP tool, so it supplies the instruction and the measured settings and the agent does the fetching. **SessionStart** says to call `memory_digest` once with a `tokenBudget`, index the result before reading any of it (the digest returns every memory three times), and read in full only what matches the task; it also carries the retrieval settings that were measured against this store — `minScore` 0.65 unfiltered, `tags: ["project:<slug>"]` to scope, never both together, and how to tell a threshold-clipped hit from noise in the `scoreBreakdown`. **Stop/SubagentStop** asks the other half: write back what is durable with `memory_write` (an `entityKey` so a new version supersedes the old, plus a `project:` tag), and correct anything the session CONTRADICTED in the same turn — a cleared blocker, a renamed project, a changed environment fact. It detects whether the store was consulted at all from a bounded 256 KB tail of this session's transcript (tool names only; the text is never stored, printed or hashed) plus a session-bound marker, so a long session that read it early still counts. It **never blocks** — it cannot know whether a given session needed the store — and stays completely silent on a machine with no Synapse (`%USERPROFILE%\.synapse\synapse.db`, or `SYNAPSE_HOME` for a non-default layout). |
| `Session-Summary-Check` | pre-task (SessionStart, UserPromptSubmit) | **Advisory — it never blocks.** It asks for the **closing section of the agent's final reply**: a short, honest wrap-up in the user's language in two parts — **DONE** (what actually shipped **and** was verified this session) and **REMAINING** (what is still open, blocked or deliberately deferred, and why) — placed after the `MCP used:` line, the `Skills used:` line and the rules confirmation the sibling hooks ask for. The message states the timing rule (only in the message that finishes the task, never mid-work, and only once), demands that verified and unverified claims are told apart ("the tests pass" needs a run the agent actually saw; a skipped check belongs under REMAINING), that failures, blocked steps and untested paths go in, not out, and that every gate that blocked during the task is accounted for — resolved, or still open with the reason. It also names the sibling gates whose **stop-block markers** (`Set-StopBlockMarker`, `hooks/_hooklib.ps1`) show they blocked earlier in **this** session; the markers hold the session id, so an earlier session's marker is ignored, and they are only ever read. **Delivered before the task, never at Stop — and that is the whole lesson of this hook.** On Claude Code a Stop hook's `additionalContext` re-invokes the model, so a summary asked for at Stop always arrived as one more turn *after* the work, and when nothing was left to say that turn was "waiting." or "done." — two shipped revisions looped exactly like that, and a cooldown made it rarer, not right. Delivered at SessionStart (always: a started, resumed, cleared or compacted session is a rebuilt context) and on the prompt (re-armed per session after `COOLDOWN_MINUTES`, default 15), the reply that ends the task carries the summary itself with no extra turn — the way the `MCP used:` and `Skills used:` requirements have always been delivered. Stop and SubagentStop are silent, so an older registration that still names them gets nothing. It reads no project content and writes only its own delivery stamp under Hook Maker's state directory. There is deliberately no enable/disable flag: remove the hook if it is unwanted rather than leaving it installed and mute. |
| `Cloudflare-Deploy` | post-task (Stop) | In Workers projects (wrangler config present), first checks **release readiness** — a deterministic gate, never based on hook registration/menu order, since matching-event hooks may run concurrently: stays completely silent (no reminder shown at all this Stop) unless the working tree is clean, the exact commit is known to be pushed (any configured upstream, not only GitHub), CI (if this repo has `.github/workflows`) is verified green for that exact SHA, and — when `Test-Temp-Cleanup` is installed for this project — it has reported a **fresh `clean`** result for the *current* repo state. `clean` is the only release-ready category: `review-required`, `residue-confirmed`, `partial` and `unknown` all keep this hook silent, as do a stale fingerprint, a missing record, and any value this build does not recognize (a race on the same Stop just means it re-evaluates on the next one, never a loop). The practical consequence of requiring `clean`: a project that permanently carries an untracked review artifact — a `coverage/` directory, say — reports `review-required`, so this hook now stays silent where an earlier version would have offered to deploy. Once ready, it walks the AI through a **deployment-worthiness decision** (deploy only when the task is complete, tests/typecheck/lint/build pass, the exact release commit is known, CI is green if the repo uses CI, no secrets/local-only files, unrelated changes, or disposable test cache/temp residue are included, the environment/bindings/migrations are understood, and project rules permit it — otherwise finish without deploying), **explicit environment selection** (never silently defaults to production), a **conditional Cloudflare-specific pre-deploy review** (Worker name, env vars, bindings, D1/KV/R2/Queues/Durable Objects + migrations, service bindings, routes, cron, compat flags, CLI version, build output — only what's relevant to the diff), and **required post-deployment verification** (record environment/Worker/exact commit SHA/deploy command (no secrets)/version or URL, then smoke-test or otherwise confirm it — a zero exit code alone is never treated as proof, and a failure is never hidden or blindly redeployed). |
| `Skills-Check` | pre-task (SessionStart, UserPromptSubmit) + post-task (Stop, SubagentStop) | Covers the whole skill lifecycle, routed to the **active agent's** locations and never to both policies at once (`skill-policy.md`, or `skill-policy-codex-optimized.md` under Codex). Three **installed** sources are always enumerated together — a skill missed because its source was never scanned is the defect this hook exists to prevent: GLOBAL `~\.claude\skills` (Codex: `~\.agents\skills`), LOCAL `<project>\.claude\skills` (Codex: `<project>\.agents\skills`), and PLUGIN `~\.claude\plugins\cache\<marketplace>\<plugin>\<version>\skills\*`, reported as `<plugin>:<skill>` because that is how the client addresses them. A fourth, **uninstalled** source — the external library named by `SKILLS_DIR` / `AI_SKILLS_DIR` (default `G:\Program Files\Portable\Scripts\.SKILLS`) — is searched against the current prompt. SessionStart gives the routed inventory (project + global deduped by the `name:` in each `SKILL.md`, plugin skills summarised by plugin and count) plus the closing requirement. `UserPromptSubmit` prints a prompt-matched shortlist: installed matches to activate by their exact `name:` (no authorization needed — they are already loadable), and library matches that are **not** installed, each with the exact `Copy-Item -LiteralPath "<library path>" -Destination "<project skills dir>" -Recurse -Force` command plus the import guidance (real folders, never junctions or shortcuts; recorded in `.ai/SKILLS.md`). **The hook never copies, installs, refreshes, overwrites, enables or removes a skill, and never writes inside the scanned repo** — importing is an authorized operation, so discovery plus an exact instruction is the most it may do. `Stop`/`SubagentStop` **blocks once** on one confirmed condition: the transcript shows a `Skill` tool call was actually made and the closing summary carries no line starting `Skills used:`. Whether a skill *should* have been used is a judgement the hook cannot make, so a session that invoked none gets a non-blocking advisory instead; an unreadable transcript is UNKNOWN and also advisory. The detector is anchored to the start of a transcript line so the hook's own instruction text can never satisfy it. Only skills **actually invoked** or materially followed belong on the line — never one merely installed, available, discovered, copied, considered or read — and `Skills used: none - <one-line reason>` is a valid answer; a skill is never forced onto a trivial task just to produce the line. Token- and time-efficient: silent when no skill source exists at all, and the two expensive enumerations (the plugin-cache glob and the ~1000-entry library walk) are cached behind a TTL while the small curated project/global directories are always read live. `.env`: `SKILLS_DIR`, `GLOBAL_SKILLS_DIR`, `PLUGIN_SKILLS_ROOT`, `LIBRARY_INDEX_TTL_MINUTES` (default `1440`; `0` disables expiry, and the index is rebuilt anyway when the configured paths change), `SKILLS_SUMMARY_ENFORCEMENT` (`block` default \| `advisory` \| `off`). |
| `Mcp-Usage-Check` | pre-task (SessionStart, UserPromptSubmit) + post-task (Stop, SubagentStop) | SessionStart states the compact MCP policy **and the closing requirement** up front, so the requirement is never a surprise at Stop. `UserPromptSubmit` reminds again **only when the prompt itself suggests docs/browser/database/GitHub/other MCP use would materially help** (once per session, silent for prompts with no such signal) and records the session as MCP-relevant, so the closing half can tell “nothing was needed” from “something was needed and skipped”. `Stop`/`SubagentStop` **blocks once** on exactly one confirmed, reproducible condition: this session really did call a connected MCP server tool — matched as the client's own `"name": "mcp__<server>__<tool>"` tool-call record, never as a bare mention in a prompt or a comment — and the closing summary carries no line starting `MCP used:`. The block names the servers it observed and the exact line to add. The **reverse** case (no MCP tool called at all) stays a non-blocking advisory even in block mode, and is raised only for a session the prompt half already flagged relevant: the hook cannot prove a tool was *needed*, and a gate that fires on suspicion is forbidden. An absent or unreadable transcript is UNKNOWN — stated as a plain advisory that says so, never an all-clear and never a block; that is also the permanent state on a client that supplies no transcript. The detector is anchored to the start of a transcript line, and the hook never prints the `mcp__` prefix it searches for, so it can neither satisfy nor arm itself from its own output. `MCP used: none - <one-line reason>` is a valid answer. `.env`: `MCP_SUMMARY_ENFORCEMENT` (`block` default \| `advisory` \| `off`). |
| `Large-File-Check` | pre-task (SessionStart, UserPromptSubmit) + post-task (Stop) | **Advisory only — never splits a file, never blocks a push, and never blocks completion: the Stop report is a non-blocking, client-aware advisory (Claude `hookSpecificOutput.additionalContext` / Codex `systemMessage`), never `decision:block`.** Pre-task: injects "design correctly from the start" guidance so new code lands in the right module up front instead of growing one huge file and decomposing later (SessionStart = full policy, UserPromptSubmit = shorter reminder, both quoting the effective threshold). Post-task: scans source files **strictly greater than** `LINE_THRESHOLD` (default **800**, so 801 is reported and exactly 800 is not), lists the largest offenders, and asks the AI whether a split is safe and worthwhile — old files present before the task are scanned too, 801 never forces a split, and the AI decides by responsibility and cohesion (~800 lines is a review signal, not a law; never make thin wrappers just to duck it). Reparse points never followed — including when the project root itself is a junction/symlink (refused, never walked) — and files >3 MB skipped. `MAX_FILES` is a real **per-file** stop; reaching it is reported as partial coverage, and a partial scan that finds no offender still emits a short non-blocking "coverage was incomplete" advisory instead of a silent false all-clear. The walk is also bounded on **directories traversed** (`MAX_DIRECTORIES`, default 4000) and **wall time** (`MAX_SCAN_SECONDS`, default 5s) so a huge non-source/asset tree can't make it run unbounded; hitting either is reported as partial coverage naming the exact limit (files / directories / time / read failure). `-GitPrePush` exits 0 without scanning or blocking. Threshold/extensions/cooldown/ceilings (files, directories, time) configurable in `.env`. |
| `Dependabot-Check` | pre-task (SessionStart) | In a GitHub repo, reports pending pull requests from the verified `app/dependabot` author (with exact head SHA, classification, merge/check state) so they are reviewed before unrelated work. Detection only — never merges. |
| `Ci-Status-Check` | post-task (Stop) | After a push, blocks "done" until checks for the **exact** pushed commit reach an acceptable terminal state. Pending/failed authorization remains blocking even while repeated detail is cooled down; GitHub queries are throttled, exact-SHA, and bound to the SAME resolved remote used for both repository selection and pushed-state verification (never a mismatched `@{upstream}`). Missing access is never reported as verified. When CI is blocked only by a confirmed **external** condition (GitHub outage, no hosted runner, an org/repo permission failure, an externally-controlled secret being unavailable, a manual approval/environment gate, ...), the agent can record an explicit, evidenced, local-only exception bound to the exact repository + pushed SHA: `-ReportExternalBlocker -Classification <category> -Reason "<evidence>"`. **Eligibility is conservative:** an exception may only be recorded when the observed GitHub run state is non-code-failure identifiable without reading logs — still pending / a manual gate, or a completed run whose conclusion is `cancelled`/`timed_out`/`stale`/`action_required`. A completed `failure`/`startup_failure` is **never** eligible (the hook cannot tell a real code/test/build/lint failure from an external one without trusted evidence), regardless of classification (including `other-external`); the classifications describe *why*, but cannot excuse a generic `failure`. **One narrow, evidence-backed exception is auto-detected:** an account **billing/payment/spending-limit** block. When Actions cannot start for that reason GitHub reports every run as `failure` even though no job ran — status alone is indistinguishable from a real failure, which is why it is otherwise refused. The distinguishing evidence is GitHub's **own check-run annotation** (*"recent account payments have failed or your spending limit needs to be increased"*), read from the check-runs annotations API — never inferred from step counts (a broken workflow file also yields zero steps but a *different* annotation, so it still hard-blocks). It is recognized only when **every** failing check-run for the commit carries that annotation, verified **pagination-safe** (all check-run pages and all annotation pages are read up to a strict bound, `total_count` is confirmed against the number actually fetched), and it **fails closed** on any query error, a truncated/unverified page, an oversized set beyond the bound, or a single failing check-run without the annotation. This case is auto-recorded as `account-billing` (no `-ReportExternalBlocker` needed), reuses the same throttled recheck/retire machinery, and still never marks CI green — no commit can clear a billing block. Recording **always queries the exact-SHA CI state first** — refused if already green, refused on a genuine completed failure, refused if CI can't be queried — and captures a non-secret CI fingerprint (`databaseId`/`attempt`/`workflowName`/`status`/`conclusion`/`updatedAt`, so a rerun is distinguishable). It never marks the commit verified/green; **while active, Stop emits a non-blocking "CI NOT VERIFIED GREEN" notice — once per session per blocker** — so the final task context cannot misrepresent CI as successful. Once, because on Claude Code a Stop `additionalContext` re-invokes the model: emitted on every Stop, this notice re-invoked the agent on every Stop (answer, stop, re-invoke, answer, until the user interrupted) in every repository with a billing block, which stays valid for a day. A new session, a new pushed commit or a changed blocker is told again. The notice shape is client-aware: **Claude Code** receives `hookSpecificOutput.additionalContext` (the officially documented model-visible field for Stop), while **Codex** receives `systemMessage` (its only documented common Stop output field — surfaced to the user/event stream; Codex does not document model-visible Stop context, so this is the strongest verified non-blocking option there). Neither shape ever uses `decision: "block"`. The client is detected the same way as the rest of this project (`CLAUDE_PROJECT_DIR` present → Claude, absent → Codex). Every later Stop re-verifies on a throttle (`EXTERNAL_BLOCKER_RECHECK_MINUTES`, default 15): the same fingerprint keeps completion allowed, CI turning green retires the exception and verifies normally (no external wording), and CI changing to a different state invalidates it and blocks again. Expires after `EXTERNAL_BLOCKER_TTL_MINUTES` (default 1440); a new commit or different repository invalidates it immediately. This is not a log-analysis subsystem. |
| `Github-Baseline-Check` | pre-task (SessionStart) | Checks that workflows contain blocking project validation (not empty/deploy-only YAML) — recognizing block-style, flow-style (`on: [push, pull_request]`), bare-value, and block-sequence triggers scoped to the **direct children** of the top-level `on:` block (an unrelated nested `push:` step input, e.g. `docker/build-push-action`, or a trigger word nested deeper as an option such as a `push` key under `workflow_dispatch: inputs:`, is never mistaken for a trigger), and multiline `run: \|` validation blocks (skipping full-line shell comments, so a keyword appearing only in a comment like `# TODO: run npm test` is not counted). A `workflow_call` reusable workflow counts as CI only when a directly-triggered local workflow actually `uses:` it (an uncalled reusable workflow is reported as a gap, not assumed complete). Flags broad `continue-on-error` and unsafe PR-target execution, detects common ecosystems in pruned deep monorepos (a bare `pyproject.toml` defaults to the `pip` Dependabot ecosystem, which also covers Poetry; reclassified as `uv` only when `uv.lock` is present), and validates `dependabot.yml` directory/directories coverage. Deliberately does not evaluate `if:` step conditions, check least-privilege `permissions:`, or confirm a reusable workflow called only from another repository — a lightweight, deterministic heuristic, not a full YAML/static analyzer. |
| `Rules-Check` | pre-task (SessionStart, UserPromptSubmit) + post-task (Stop, SubagentStop) | Before the task it lists the rules that govern this project: the **global** rules directory (`~\.claude\rules` / `~\.codex\rules`) plus the current project's **local** rules directory (`<project>\.claude\rules` / `<project>\.codex\rules`). First check lists all rules files; afterwards it stays silent until a rules file is added/changed/removed, then reports exactly what moved. Detects the running client automatically (Claude Code exports `CLAUDE_PROJECT_DIR` on hook processes; Codex does not). **After** the work it checks that the closing summary actually *confirms* which of those rules were read and applied: listing rules at the start is a request, this half is the verification, and it is the reason the hook exists at all. `Stop`/`SubagentStop` **blocks once** when all three of these hold: rule files exist for this project, the session transcript shows this session really **edited files** (the client's own `Edit`/`Write`/`MultiEdit`/`NotebookEdit`/`str_replace*` tool-call record), and no line starts `Rules applied:`. The block lists the governing rule file names (capped, `+N more` past 14), so the confirmation is drawn from a real menu instead of being the bare “done” acknowledgement the rules forbid. A read-only or advice-only session gets a non-blocking advisory instead — the hook cannot show the rules were load-bearing there — and an absent or unreadable transcript is UNKNOWN, also advisory, never an all-clear. `Rules applied: none - <one-line reason>` is a valid answer, and naming a rule that was not read is worse than admitting it was skipped. The detector is anchored to the start of a transcript line so the hook's own instruction text can never satisfy it. `.env`: `GLOBAL_RULES_DIR`, `RULES_CONFIRMATION_ENFORCEMENT` (`block` default \| `advisory` \| `off`). |
| `Secrets-Check` | pre-task + post-task (Stop) | Safely scans real `.env*` files throughout active project subtrees (templates, dependencies, builds, caches, assistant runtimes, and reparse points are pruned). Lifecycle events retain registry/placeholder/unused advisories. The leak scan merges and deduplicates git **working-tree and index** matches (`git grep` + `git grep --cached`), so a value staged then cleaned from the working copy only — or committed and later edited away locally without staging that edit — is still caught. During native pre-push it **additionally scans the exact commits about to be pushed**, resolved from git's real pre-push ref-update stdin (`<local ref> <local sha> <remote ref> <remote sha>`): a secret sitting in an outgoing commit is caught even when the working tree and index are already clean. The outgoing-history scan does **not** exclude `.env*`/`secrets.md` — a committed real `.env` or registry file in outgoing history is itself a leak, even if it was later deleted/untracked before the push. New branches (all-zero remote sha), deletions (all-zero local sha), force-pushes/non-fast-forwards, and multiple refs in one push are all handled. It **fails closed**: the whole range is scanned in bounded batches (no commit cap), and if any ref range cannot be resolved (unresolvable remote sha, `rev-list`/`git grep` error) the push is **blocked** with a safe incomplete-scan message rather than treated as clean. Native pre-push blocks only confirmed indexing/leak/outgoing/incomplete-scan risks; advisory-only findings exit successfully. Secret values are never printed. The leak scan is grouped **by value, not by key**: several keys holding one value (`ADMIN_EMAIL`/`OWNER_EMAIL`/`KYC_ADMIN_EMAIL`) produce one finding naming all of them, instead of the same file list repeated per key. **Classification precedence:** `SECRET_KEYS` override → *definite* credential value formats (private key, JWT, `sk_live_`, `ghp_`, `AKIA`, `user:pass@host`, credential query params) → credential-like key semantics (`*_PASSWORD`, `*_TOKEN`, `*_SECRET`, `API_KEY`, …) → `PUBLIC_CONFIG_KEYS` override → the *entropy heuristic* (24+ chars, no whitespace, mixed case + digit) → inferred public config → Unknown. The two value tiers are deliberately unequal: a definite format **identifies** a credential and can never be declassified, but the entropy heuristic only **guesses**, and it fires on values that are public by design — a Turnstile **site** key, a Cloudflare account ID, a publishable key — so it sits *below* `PUBLIC_CONFIG_KEYS` and can be corrected per key. Its default answer is unchanged (still Secret with no override). An e-mail address is excluded from the heuristic outright; an address under a credential-named key (`SMTP_PASSWORD`) is still Secret via key evidence. **Limitation:** `Secrets-Check` is a value-based leak guard, not a full entropy/pattern/history secret scanner — the only source of searchable values is the active `.env*` files. `secrets.md` is read to check whether a key is documented and for the unused-key scan; its recorded values are **not** themselves leak-scanned, so a secret that lives only in `secrets.md` or only in the deployment secret store is invisible to it. A secret introduced and later removed from outgoing history **and** every current source is likewise no longer known and cannot be matched; use an established dedicated historical-secret scanner in CI for unknown credentials. |
| `Ignore-Rules-Check` | pre-task (SessionStart) + post-task (Stop) + native Git pre-push | Auto-adds required local/private patterns to `.gitignore`, extracts additional paths from explicit local-only/never-commit project rules, and blocks completion while required fixes remain. Default public template files (`.env.example`/`.env.sample`/`.env.template`/`.env.dist`) are intentionally allowed to stay tracked via `!`-negation exceptions — they are never reported as a protected path that must be untracked, while real env files (`.env`, `.env.local`, `.env.production`, ...) remain protected. Patterns are written and compared in **insertion order**, never alphabetically sorted (an alphabetical sort would silently break every negation exception, since `!` sorts before `/`), and a path whose effective `git check-ignore` match is a negation is always treated as explicitly allowed. Project installs create a deterministic pre-push chain: Ignore → Secrets → Utf8 → preserved previous hook. Large-File-Check remains advisory and never decides whether a push may proceed. Optional extra patterns can be set in `.env`. |
| `Dependency-Version-Check` | pre-task (SessionStart, UserPromptSubmit) + post-task (Stop, SubagentStop) | **Advisory only** — never modifies manifests/lockfiles/workflows/Dockerfiles, never runs an upgrade, never auto-merges Dependabot PRs, never blocks completion. SessionStart reports version-freshness findings for ecosystems actually detected in the project: real `npm outdated --json` (exits 1 when outdated packages exist — treated as success, not a failure) and, for Python, `python -m pip list --outdated --format=json` run through **this project's own interpreter** (`.venv`/`venv`, or `VIRTUAL_ENV` only when it points inside the project) rather than whatever `pip` resolves to on PATH — the machine's global packages are not this project's dependencies, and reading them produced findings that were not merely noisy but backwards (a project venv can be *ahead* of global). A project with Python manifests but no environment of its own is reported **incomplete**, never answered from global. These classify each update as patch/minor/major/prerelease; GitHub Actions get a static known-minimum-major check on `uses:` refs; runtime pins (`.nvmrc`/`.node-version`/`.python-version`/`engines.node`) and Docker base images (`FROM` tags) are checked against a small known-EOL list, and a floating/untagged image is flagged as non-reproducible. Ecosystems without a verified, reliable JSON command here (pnpm, Yarn, Bun, Poetry, uv, .NET, Java/Gradle/Maven, Rust/Cargo, PHP Composer, Ruby Bundler) are detected and reported as an **incomplete** check with the exact command to run manually, rather than guessing at an unconfirmed schema or a fragile text-table parse — it never claims a dependency is current when a check could not run. `UserPromptSubmit` reuses the cached findings and additionally injects "prefer the latest stable compatible version, not a prerelease" guidance when the prompt looks like it's adding a dependency/framework/runtime/build tool/action/Docker image or starting a project from scratch. Cached per-project by a fingerprint of detected ecosystems + manifest/lockfile/workflow/Dockerfile/runtime-pin **content** hashes (not just presence), so an unrelated task stays fast while any real change triggers a fresh scan; default cooldown is 7 days. Complements `Dependabot-Check` (existing PRs) and `Github-Baseline-Check` (CI/Dependabot baseline structure) without duplicating either.  `Stop`/`SubagentStop` replay the still-unanswered findings **once per session**, from the cached report only — no scan, no `npm`/`pip`/`go` invocation, no upgrade, and **no block on any event**: whether to upgrade is the user's decision and a judgement the hook cannot make. What changed is the output contract, not the scanning — the report now asks the closing summary to carry a line starting `Dependency decisions:` answering each finding with `<name>=update\|keep\|defer (<reason>)` (or `Dependency decisions: none - <one-line reason>`). That line is *requested*, never enforced: an unanswered finding is reported as an unreviewed risk, not as a refusal to stop. `.env`: `COOLDOWN_MINUTES` (default `10080` = 7 days), `MAX_FINDINGS` (default `12`), `CLOSING_REMINDER` (`1` default; `0` keeps the hook pre-task only). |
| `Test-Temp-Cleanup` | pre-task (SessionStart) + post-task (Stop; SubagentStop opt-in) | **Detector + advisor + completion gate — it deletes nothing.** There is no deletion feature and no setting that can enable one: the hook never deletes, moves, renames, truncates, stages or otherwise mutates any project file, never runs `git clean`/`reset`/`rm`/`add`/`restore`/`checkout`, and never touches the git index. The only thing it writes is bounded metadata into Hook Maker's own private state directory under `%LOCALAPPDATA%\HookMaker\state` — never candidate file contents, secret values, prompts, tool inputs or raw logs. The **agent** performs any deletion, with its normal tools, after independently confirming each candidate. It recognizes disposable test cache/temp residue (`.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `.hypothesis`, `.nyc_output`, `.jest-cache`, `.vitest-cache`, `.test-tmp`/`.test-temp`, and `__pycache__`/`*.pyc`/`*.pyo` **only** when they genuinely appeared during this session — a pre-existing one is `ambiguous`, never disposable) and diagnostic/review artifacts (`coverage`, `htmlcov`, `test-results`, `playwright-report`, `blob-report`, `TestResults`, `.coverage`, `coverage.xml`). SessionStart records a metadata-only baseline (relative path, kind, link state, size, mtime, git state) plus the scan's own completeness and its exact partial causes, staying silent unless that scan was itself partial. Stop rescans under the same bounded rules, compares against this session's baseline, and gives every candidate exactly one classification — `likely-disposable` (a *description*, never deletion approval), `review-or-diagnostic`, `protected`, `ambiguous`, `unknown-git-state`, `partial-or-unreadable`. Hard protections, none of them overridable by config: the hard-prune set (`.git`, `.ai`, `.claude`, `.codex`, `.kiro`, and dependency/build roots such as `node_modules`, venvs, `vendor`, `target`, `dist`, `build`, `out`) is never surfaced and never descended into — a directory is also pruned by a MARKER FILE it contains, so a name list cannot miss it: `pyvenv.cfg` (PEP 405) means an unconventionally named virtualenv like `spotdl-env` is pruned exactly like `.venv`, and `CACHEDIR.TAG` means any tool's regenerable cache is skipped whatever it is called. A reparse point is never probed for a marker; symlinks/junctions/reparse points are never followed; nothing outside the canonical project root is surfaced; tracked and staged paths are protected; and anything with incomplete metadata or unknown git state is never described as disposable. The result is one of five categories — `clean`, `review-required`, `residue-confirmed`, `partial`, `unknown` — and **a partial scan can never report `clean`**: coverage limits and read failures resolve to `partial`, missing/unusable git or baseline evidence to `unknown`. A candidate it already surfaced as likely-disposable on an *earlier* Stop of this session that is still present becomes `residue-confirmed`, which is evidence its instruction was not resolved, **not** a claim that the hook confirmed the path is safe to delete. `residue-confirmed` and `partial` gate completion; `review-required` and `unknown` are advisory. Gating is fingerprinted on the bounded evidence, so unchanged evidence never loops while materially changed evidence re-surfaces immediately. Configurable in `.env`: `MAX_SCAN_ENTRIES` (default 15000), `MAX_SCAN_DEPTH` (12), `MAX_FINDINGS` (20 — truncates the report list only, never the scan), `ENABLE_SUBAGENT_STOP`, and `EXTRA_CANDIDATE_NAMES`/`EXTRA_REVIEW_NAMES`, which are **detection hints only** — config can add something to look at, never mark something safe to delete and never weaken a protection. The coordination record `Cloudflare-Deploy` reads is written on every Stop, even when the message itself is suppressed (see above). |
| `Test-Plan-Check` | pre-task (SessionStart, UserPromptSubmit) | The **before** stage of the three-stage test-health architecture (`Test-Plan-Check`, `Test-Run-Guard`, `Test-Completion-Check`). **Detector + advisory: it never executes a test, spawns a process or invokes a runner, never writes anything into the project (its only state lives under `%LOCALAPPDATA%\HookMaker\state`), and never blocks** — it emits advisory context or nothing at all. SessionStart surfaces the applicable policy once per project/state. `UserPromptSubmit` is **relevance-gated**: it reacts only to prompts that are actually about tests, CI, runners, hangs, timeouts, parallelism, workers, CPU/memory, sleep/polling or test cleanup — ordinary prompts get nothing, and silence is by far the common case. A **standalone** `::deep-debug` token (never the prose "deep debug") also passes the gate and extends the same advisory with the deep-debug test-health plan requirements under the same fingerprint gate; that is detection only — the hook never executes a codeword, slash command or skill. Reported risks are only ever things it can **point at**, with a file and line, in the repository as it exists right now: a fixed blind sleep at or over the blind-wait ceiling inside a test file, an unbounded wait, an unbounded polling loop, a child process started with no visible wall/idle bound. Never a theoretical warning. The scan is an iterative directory walk that **prunes** excluded trees (`.git`, `node_modules`, `.venv`, `dist`/`build`, `graphify-out`, `.ai`, `.claude`, ...) and never follows a junction or symlink; separate ceilings bound directories visited, candidate test files inspected, KB read from any one file, total wall time and findings emitted, and hitting any of them — or an unreadable directory — makes the advisory say the scan was **partial** rather than implying the whole repository was covered. Token-efficient by design: a compact fingerprint (git state plus a signature of what the scan actually saw) and a cooldown keep an unchanged repository from re-warning on every prompt, while a **changed** state is reported immediately, ignoring the cooldown — that is the whole point. Output goes through the shared `Write-HookResult` adapter rather than a hand-rolled client branch, and no path ever emits `decision:block`. `.env`: `TEST_PLAN_COOLDOWN_MINUTES`, `TEST_PLAN_EXTRA_KEYWORDS` (project vocabulary on top of the built-in relevance set), `TEST_PLAN_ALWAYS_REPORT` (ignore the cooldown; for debugging the hook itself), the scan ceilings `TEST_PLAN_MAX_DIRS` / `TEST_PLAN_MAX_FILES` / `TEST_PLAN_MAX_FILE_KB` / `TEST_PLAN_MAX_SCAN_SECONDS` / `TEST_PLAN_MAX_FINDINGS`, and `TEST_PLAN_MAX_BLIND_SLEEP_SECONDS` (`0` disables the check), which deliberately mirrors `TEST_GUARD_MAX_BLIND_SLEEP_SECONDS` in `Test-Run-Guard` — same value and same `0`-disables meaning, so the before-stage and the during-stage cannot disagree about what a blind sleep is; change one, change the other. An invalid value is reported once in plain text and the default is used: a malformed setting must never block or crash a session. |
| `Test-Run-Guard` | pre-task + post-task (PreToolUse, PostToolUse) | The **during** stage of the three-stage test-health architecture. `PreToolUse` is a **gate** that blocks exactly one thing: a clearly-recognised raw test command about to run with no bounded runner around it — and it answers with the exact safe replacement. `PostToolUse` is a **detector**: it reads the guarded runner's structured result and reports what actually happened (timeout, kill, leaked process, non-zero exit) and never blocks. **Why the hook does not watch the run:** the hook process returns in milliseconds and is gone long before a suite finishes, so the watchdog lives in `scripts\Run-Tests-Guarded.ps1`, which owns the child process for its whole life; the hook only decides whether that owner exists and later reads what it recorded. **Recognition is conservative and structured.** The command is treated as data: tokenized with quotes respected, split on separator tokens, and matched on the program token plus, where it disambiguates, its subcommand. It is never evaluated, never concatenated into a shell string and never handed to `Invoke-Expression` — this hook starts no process at all. A `pwsh -Command '<string>'` payload is deliberately **not** inspected, because recognising it would mean parsing a shell, so it stays silent; anything not positively recognised produces total silence, since a false positive here would block real work. It also denies a direct **blind wait** — a fixed literal delay at or above the ceiling (`Start-Sleep -Seconds N`, `sleep N`, `timeout /t N`), or an always-true poll loop that sleeps with no visible deadline — and answers with a deadline-bounded readiness check. That detection inspects only **top-level** tokens, so a delay nested inside a quoted string (`bash -c "sleep 300"`) is never parsed, a short backoff below the ceiling is never touched, and GNU `timeout 300 <cmd>` bounds a command rather than being a blind wait. A deny or advisory also states the bounds the replacement already enforces (bounded runner, the documented native timeout where the framework supports one, wall/idle limits, one shared worker ceiling, process-tree cleanup, exit-code propagation, no ad hoc sleeps) and — only when the command **visibly** writes textual output to a file — advises explicit UTF-8 output using officially documented syntax only (PowerShell `-Encoding utf8`, Python `PYTHONUTF8`/`PYTHONIOENCODING`); it never invents an encoding flag, and validating files stays `Utf8-Encoding-Check`'s job. **Coordination state for `Test-Completion-Check`** is written per run, keyed by `<projectKey>-<runId>`, so two concurrent guarded runs in one project never overwrite each other's evidence: `TestRunGuard-observed-<key>-<runId>.json` is written on `PreToolUse` for **every** recognised test command — blocked, advised or already guarded, recording which — and is what lets `Test-Completion-Check` refuse a "tests passed" claim when nothing produced a result; a blocked command is recorded too, because the intent to test is what creates the obligation to show evidence. `TestRunGuard-result-<key>-<runId>.json` is written by the runner itself through the per-run `-ResultPath` this hook puts in the replacement, and `PostToolUse` pairs a result to its observation by run identity. The `active` marker is deliberately **not** written here: only the runner knows its own live pid, an absent file means "not evaluated" and is therefore silent, whereas a guessed pid would be a false completion blocker on a recycled process id. **Worker ceiling:** the runner resolves one effective cap — this project's value folded with the shared resource-aware budget `max(2, min(8, cores-2))` and any stricter pre-existing value, so it only ever tightens — and exports it as `HOOKMAKER_MAX_TEST_WORKERS` into the child's environment. It does **not** rewrite the command's own worker flag, since every framework spells that differently (`pytest -n`, `jest --maxWorkers`, `vitest --maxThreads`, `go -p`, `cargo -j`, `dotnet -m`, PowerShell `-ThrottleLimit`) and the command that oversubscribes usually carries no flag at all; so the cap binds inside a runner that reads the variable (this repository's `scripts\Run-Tests.ps1` does), and a framework that ignores it stays advisory. **Progress, not liveness:** a run counts as progressing while *any* of output bytes, cumulative CPU in the owned process tree, owned tree membership, or an optional heartbeat file advances; only when none of them move for the idle window is it terminated — so a silent but CPU-busy computation is never killed for idleness, and only the wall and memory ceilings bound it. `.env`: `TEST_GUARD_WALL_TIMEOUT_SECONDS` (default `1800` — a ceiling, not an expected duration), `TEST_GUARD_IDLE_TIMEOUT_SECONDS` (`300`; `0` disables), `TEST_GUARD_HEARTBEAT_SECONDS` (`10`), `TEST_GUARD_MAX_MEMORY_MB` (`0` = disabled on purpose: there is no universal correct value, and a wrong one kills valid work on a large suite), `TEST_GUARD_MAX_WORKERS` (empty = the shared budget), `TEST_GUARD_MAX_BLIND_SLEEP_SECONDS` (`30`; `0` disables), `TEST_GUARD_EXTRA_TEST_COMMANDS`, `TEST_GUARD_NEVER_GUARD` (takes precedence over recognition), `TEST_GUARD_ADVISORY_ONLY` (`0`). An invalid value is reported once in plain text and the default used: a malformed setting must never turn this gate into an unconditional blocker, and must never silently disable it either. |
| `Test-Completion-Check` | post-task (Stop, SubagentStop) | The **after** stage of the three-stage test-health architecture, and a **gate**: it blocks completion only on a confirmed, current-state condition, and it never runs a test, never spawns a process and never edits a file — its own state lives under `%LOCALAPPDATA%\HookMaker\state`, never in the project. The recursion guard comes first: `stop_hook_active` is deliberately NOT the guard: it is set for **any** gate's block, so exiting on it would make one gate silence the others. This gate stands down on **its own** block marker instead, which bounds it at one block per session. It previously had no guard of either kind and so refused completion on every Stop, unbounded - a Stop gate that can re-trigger itself is a hang, not a check. **What it blocks on, each confirmed from recorded evidence and never inferred:** a guarded run is still active (its recorded owner pid is alive); the guarded result says `terminated` (wall timeout, idle timeout or memory limit) and that incident is not yet resolved; the result carries a non-empty `leakedProcessIds`; the result says `failed`, so the run did not complete cleanly; a test command was **observed** for the current project state but there is no current guarded result to prove how it ended; or a durable `.ai/` note is owed for a hang, timeout, kill or leak and has not been written yet. Everything else is silence — no relevant test work is by far the common case. **Staleness only weakens positive evidence, never negative findings:** a result older than the evidence window is not proof that a run happened for the current state (the "no result" case then applies), but an old hang is still an unrecorded hang, so the termination and leak cases are evaluated regardless of age until resolved. It **never** says "all tests passed": it reports only what the recorded evidence actually shows, and stays silent rather than claim a scope it has not seen. Every coordination file it consumes is **optional** — an absent file means that check is simply not evaluated, so a missing producer can never widen what this hook blocks on — and the per-run result/observed/active files are enumerated and aggregated across a project rather than read from one path (a legacy non-suffixed file from an older build is still honoured), so completion is accepted only when every current-state observed run is finished. **`Test-Temp-Cleanup` race:** Stop hooks for one event run concurrently and independently, and registration order is display-only, never an execution order; when `Test-Temp-Cleanup` is installed for this project but has not yet recorded a result for the current fingerprint, this hook waits at most the coordination window and then **defers once** — it stays silent and re-evaluates on the next event, never retrying inside one invocation. `.env`: `TEST_COMPLETION_EVIDENCE_MINUTES` (how long a guarded result stays current for the same project state), `TEST_COMPLETION_ADVISORY_ONLY` (report without blocking; an **invalid** value falls back to advisory rather than to the blocking default, because falling back the other way would widen what a malformed setting blocks on), `TEST_COMPLETION_ALWAYS_REQUIRE_NOTE` (require the durable `.ai/` note after any guarded run, not only after a hang/timeout/kill/slow run) and `TEST_COMPLETION_COORDINATION_WAIT_SECONDS` (the one-shot deferral wait, never a retry loop). |
| `Utf8-Encoding-Check` | pre-task (SessionStart) + post-task (Stop) + native Git pre-push | Enforces the strict-UTF-8 policy for text the task writes. SessionStart records a bounded, metadata-only baseline (path/size/mtime/classification fingerprints — never file contents) and surfaces pre-existing non-UTF-8 files as a concise advisory only. Stop inspects text created/modified during the task (working tree + staged + committed-during-task) and **blocks** new/changed invalid-UTF-8 not covered by a documented exception; partial/unknown coverage is reported honestly and is never an all-clear. Strict decoder (never replacement-char decoding): UTF-8 with and without BOM both pass; UTF-16/UTF-32/Windows-1252/ANSI are non-UTF-8; a NUL-bearing binary is skipped as binary (bounded heuristic, never extension alone). Never rewrites, transcodes, normalizes line endings, or touches a BOM — it detects; the agent converts after review. Narrow documented exceptions live in an optional tracked `.utf8-encoding-exceptions.json` (exact path/pattern + encoding + concrete reason + verification; containment-checked, no `**/*`, malformed entries are rejected and grant nothing). As the third native pre-push stage it validates the exact outgoing text blobs (new branches, multiple refs, deletions, force-pushes; even files later deleted) and fails closed on invalid or incomplete required coverage. |
| `Docs-Freshness-Check` | pre-task (SessionStart) + post-task (Stop) | A mandatory review boundary for whether this task's changes made tracked, published documentation stale — never a prose rewriter: it detects and gates, the agent edits. SessionStart silently records a metadata-only baseline (starting HEAD, repo-state fingerprint, tracked public `.md`/`.txt` paths + content hashes). Stop computes the task delta from that baseline (commits + staged + working-tree changes, so it still works even if the agent already committed), classifies each changed non-doc file as a real content change or comment/blank-only noise (so pure internal refactors and formatting-only edits stay silent), and — only when real, non-excluded impact exists — blocks **once** per distinct impact fingerprint with a bounded, ranked list of candidate tracked `.md`/`.txt` files to review (conventionally-named files and files sharing a directory with the changed code rank first). Hard exclusions (never requested for review regardless of config): `.ai`/`.claude`/`.codex`/`.agents`/`.cross-project-sync`, `secrets.md`, generated/vendor/build/cache directories, fixture/snapshot/golden files, and `LICENSE*`/`NOTICE*`/`COPYING*`. The impact fingerprint is content-hashed from only the non-doc changed files, so an unrelated documentation edit never shifts it. The only way to clear a block is a fingerprint-bound acknowledgement command (`-Acknowledge -ProjectRoot <path> -ImpactFingerprint <fp> -Result Updated\|NoUpdate -Files <relative,doc,paths> -Reason "<concrete reason>"`): `Updated` requires at least one real, tracked/staged, in-project, non-excluded `.md`/`.txt` file; `NoUpdate` requires a concrete, non-generic reason (`none`/`n/a`/`done`/... are rejected). Acknowledgement is local-only state, never committed, and a later non-doc change invalidates it, requiring a fresh review. |

All advisory hooks are token-efficient by design: they stay **silent** unless a deterministic
signal fires (staleness, wrangler config, out-of-sync git, pending Dependabot PR, unverified
push), they respect a per-project **cooldown/fingerprint**, they never loop (`stop_hook_active`
guard), and the decision always stays with the AI — every reminder explicitly allows finishing
without action. The GitHub hooks degrade safely (silent, never a false "all clear") when git,
a remote, `gh`, authentication, or network access is missing.

Stop **gates** stand down on their own re-entry, never on anyone else's. `stop_hook_active` is set for *any* gate's block, so a hook that exited on that flag alone went silent whenever an unrelated hook happened to block first on the same Stop. Each gate now writes and reads its own session-scoped marker (`Test-StopStandDown` / `Set-StopBlockMarker` in `hooks/_hooklib.ps1`): one hook's block never mutes the others, and no gate repeats its own. Advisory hooks keep the plain `stop_hook_active` guard — their message has already gone out, and repeating it on every continuation Stop would be noise.

Matching lifecycle hooks for the same event run concurrently and are intentionally independent;
their registration order (and menu position) is display-only and never guarantees execution order.
`Cloudflare-Deploy` and `Test-Temp-Cleanup` hand off state through a small fingerprinted file
instead of assuming one runs "after" the other on the same Stop. The native pre-push chain is the
only ordered sequence.
It honors Git's resolved `core.hooksPath` (default, relative, nested, absolute, and paths with spaces), preserves an existing pre-push hook once, and is idempotent.

