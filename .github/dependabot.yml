version: 2
updates:
  # GitHub Actions used in workflows (no package manager: pure PowerShell scripts)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"
